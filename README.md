# BioSig
A seekable, streaming-friendly, delta-encoded binary format for physiological time-series data (EEG, ECG, EMG). biosig achieves substantial lossless compression while remaining dramatically faster than gzip for random-access window reads, with a tiny dependency-light format spec designed for large-scale biosignal archives and analysis workloads.
